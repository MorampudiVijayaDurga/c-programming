//replacing character in string
#include <stdio.h>
#include <string.h>
int main()
{
 char a[20];
 printf("enter the  string:");
 fgets(a,sizeof(a),stdin);
 int len=strlen(a);
 int i,j;
 for(i=len-1;i>=0;i--)
 {
  putchar(a[i]);
 }
 printf("\n");
 return 0;
}
