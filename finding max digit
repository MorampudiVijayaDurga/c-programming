#include <stdio.h>
int main()
{
    int num,rem,max=0;
    scanf("%d",&num);
    while(num>0)
    {
        rem=num%10;
        if(rem>max)
        {
         max=rem;
        }
        num=num/10;

    }
    printf("%d",max);

}
