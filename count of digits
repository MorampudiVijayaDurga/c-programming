#include <stdio.h>
int main()
{
    int num,i,rem,count=0;
    scanf("%d%d",&num,&i);
    while(num>0)
    {
     rem=num%10;
     num=num/10;
     if(rem==i)
     {
      count++;
     }
    }
    printf("%d",count);

}
