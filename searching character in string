//searching character in string
#include <stdio.h>
#include <string.h>
int main()
{
 char a[20],c;
 int r;
 printf("enter the first string:");
 scanf("%s",a);
 printf("enter the character you want to replace:");
 scanf(" %c",&c);
 printf("enter which index ypu want to replace:");
 scanf("%d",&r);
 int len=strlen(a),i;
      a[r]=c;

 for( i=0;i<len;i++)
 {

 printf("%s",a[i]);
}
}v
